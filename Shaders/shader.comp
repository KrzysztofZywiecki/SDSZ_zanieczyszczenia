#version 460 core

layout(local_size_x = 2, local_size_y = 2) in;

layout(binding = 0, rgba8_snorm) uniform image2D imag;

void main()
{
	imageStore(imag, ivec2(gl_GlobalInvocationID.xy), vec4(0.5, 0.0, 1.0,1.0));
}